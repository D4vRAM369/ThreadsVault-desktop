# Sidecar binaries

This folder is used by Tauri `bundle.resources` (`bin/*`) to package sidecar tools.

Expected optional binaries for ThreadsVault desktop:
- `yt-dlp`
- `ffmpeg`

In development, this placeholder keeps the glob valid when sidecars are not present.
